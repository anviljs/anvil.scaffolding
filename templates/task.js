module.exports = function( _, anvil ) {
	// to add dependencies on other tasks, just use an array
	// containing the tasks names after the function
	anvil.task( "{{{taskName}}}", "{{{description}}}", function() {
		// insert awesome task deeds here
	} );
};